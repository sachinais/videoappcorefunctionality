package com.nick.sampleffmpeg.network;

import android.content.Context;

/**
 * Created by Vindhya Pratap on 9/20/2015.
 */
public class CustomDialogs {

    public static void showOkDialog(Context context, String message){
      /*  AlertDialog.Builder alertDialog = new AlertDialog.Builder(context, R.style.Base_Theme_AppCompat_Dialog);
        alertDialog.setMessage(message);
        alertDialog.setPositiveButton("OK", new DialogInterface.OnClickListener() {
            @Override
            public void onClick(DialogInterface dialog, int which) {
                dialog.cancel();
            }
        });
        alertDialog.show();*/
    }
}
